/*
find the minimum and maximum numbers in c programming.
*/
#include <stdio.h>
#include <limits.h>

int main()
{
    int size, num;

    int max = INT_MAX; // INT_MAX means return the max number.
    int min = INT_MIN; // INT_MIN means return the min number.

    printf("MAX : %d\n", max);
    printf("MIN : %d", min);
    return 0;
}